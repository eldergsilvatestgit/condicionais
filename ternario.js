const saldo = 1;

const unidade =(saldo === 1 ?"real":"reais")


console.log(`Voce tem ${saldo} ${unidade}`);



/* if  (saldo > 1 || saldo < -1) {
   valor = "Reais"
}else{
    valor="Real"
}
console.log(`Voce tem ${saldo} ${valor}`);
 */